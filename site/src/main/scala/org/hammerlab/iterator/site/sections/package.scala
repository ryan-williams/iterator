package org.hammerlab.iterator.site

package object sections {
  val ! =
    Seq(
         count !,
        either !,
           end !,
         group !,
         level !,
      ordering !,
         range !,
        sample !,
          scan !,
       sliding !,
         start !,
          util !
    )
}
